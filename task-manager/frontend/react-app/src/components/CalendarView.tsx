import { Flex } from "@chakra-ui/react";

const CalendarView = () => {
  return (
    <Flex direction={"row"} gap={6} p={6} paddingLeft={20} pt={1}>
      📅 Vista Calendario
    </Flex>
  );
};

export default CalendarView;
